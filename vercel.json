{
  "version": 2,
  "builds": [
    { "src": "client/package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } },
    { "src": "api/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/hh/(.*)", "dest": "/api/hh/[...proxy].ts" },
    { "src": "/(.*)", "dest": "/client/$1" }
  ]
}
